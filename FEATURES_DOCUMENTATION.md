# 📘 Документация по признакам

## Обзор датасета

- **Источник:** `data/processed/transactions_with_features_final.csv`
- **Размер:** ~13 000 строк
- **Целевая переменная:** `target` (0 — нормальная транзакция, 1 — мошенничество)
- **Дисбаланс классов:** ~78:1 (больше нормальных операций)

## Категории признаков

### 1. Временные признаки
- `hour`, `day_of_week`, `day_of_month`, `month`
- Циклические компоненты: `hour_sin`, `hour_cos`, `day_of_week_sin`, `day_of_week_cos`, `month_sin`, `month_cos`
- Флаги: `is_weekend`, `is_night`

**Цель:** фиксировать нетипичное время активности.

### 2. Статистика пользователя
- `user_tx_count_total`, `user_avg_amount_total`, `user_std_amount_total`
- `user_max_amount_total`, `user_min_amount_total`
- Отклонения/отношения: `amount_diff_from_user_avg`, `amount_ratio_to_user_avg`

**Цель:** видеть аномалии относительно обычного поведения клиента.

### 3. Rolling-окна (1h / 12h / 24h)
- `tx_count_{window}`, `tx_mean_amount_{window}`, `tx_std_amount_{window}`
- `tx_sum_amount_{window}`, `tx_max_amount_{window}`
- Производные: `tx_rate_per_hour_{window}`, `avg_interval_{window}`

**Цель:** понимать динамику транзакций в ближайшее время.

### 4. Статистические признаки
- `amount_zscore`, `amount_zscore_user`
- `amount_percentile`, `amount_ratio_to_max_{window}`
- `cv_amount_{window}` (коэффициент вариации)

**Цель:** детектировать резкие скачки сумм.

### 5. Device / OS / Session
- `device_type`, `device_freq`, `user_unique_devices`, `device_changed`
- `os_version`, `unique_os_versions_30d`, `os_changed`
- `phone_model`, `unique_phone_models_30d`
- Логины: `logins_last_7_days`, `logins_last_30_days`, `avg_logins_per_day_{window}`

**Цель:** отслеживать смену устройств и частоту сессий.

### 6. Поведенческие признаки
- `time_since_last_tx`, `time_until_next_tx`
- Флаги `is_rapid_tx`, `interval_anomaly`
- Временные интервалы (`tx_in_morning`, `tx_in_evening` и т.д.)

**Цель:** выявлять подозрительные серии транзакций и интервалы.

### 7. Получатели и гео (если доступны)
- `recipient_freq_encoding`
- `ip_changed`, `user_unique_ips`, `geo_changed`

## Предобработка

- Пропуски заполнены нулями или медианными значениями.
- Числовые признаки нормализованы (MinMaxScaler).
- Целевые колоноки времени преобразованы в набор производных признаков.
- Финальный список признаков сохраняется в `model/selected_features.pkl`.

## Рекомендации

1. Для борьбы с дисбалансом используйте `class_weight` в модели или техники oversampling.
2. Валидацию лучше проводить по времени: train на старых данных, test на новых.
3. При добавлении новых признаков обновляйте `run_feature_engineering.py` и модель.
4. В ноутбуках `notebooks/02_feature_engineering.ipynb` и `03_final_model.ipynb` можно посмотреть процесс подготовки.

## Обновление документации

При добавлении новых признаков:
1. Обновите раздел соответствующей категории.
2. Пересохраните `selected_features.pkl`.
3. Перезапустите обучение и обновите метрики в `model/MODEL_DOCUMENTATION.md`.


