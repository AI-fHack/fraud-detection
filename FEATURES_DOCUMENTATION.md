# Документация по признакам для ML модели

## 📋 Обзор

Этот документ описывает признаки, созданные для модели обнаружения мошеннических переводов в Мобильном интернет-Банкинге.

**Финальный датасет:** `data/processed/transactions_with_features.csv`

## 📊 Структура данных

- **Размер:** ~13,155 строк
- **Целевая переменная:** `target` (0 = нормальная транзакция, 1 = мошенническая)
- **Дисбаланс классов:** ~78:1 (нормальные:мошеннические)

## 🎯 Категории признаков

### 1. Временные признаки (~13 признаков)
- `hour` - час дня (0-23)
- `day_of_week` - день недели (0=Понедельник, 6=Воскресенье)
- `day_of_month` - день месяца (1-31)
- `month` - месяц (1-12)
- `is_weekend` - выходной день (0/1)
- `is_night` - ночное время 22:00-06:00 (0/1)
- `hour_sin`, `hour_cos` - циклическое кодирование часа
- `day_of_week_sin`, `day_of_week_cos` - циклическое кодирование дня недели
- `month_sin`, `month_cos` - циклическое кодирование месяца

**Назначение:** Выявление временных паттернов мошенничества (например, активность в необычное время)

### 2. Признаки по пользователю (~8 признаков)
- `user_tx_count_total` - общее количество транзакций пользователя до текущей
- `user_avg_amount_total` - средняя сумма транзакций пользователя
- `user_std_amount_total` - стандартное отклонение сумм
- `user_max_amount_total` - максимальная сумма
- `user_min_amount_total` - минимальная сумма
- `amount_diff_from_user_avg` - отклонение от среднего пользователя
- `amount_ratio_to_user_avg` - отношение к среднему пользователя

**Назначение:** Выявление аномалий в поведении конкретного пользователя

### 3. Rolling-window признаки (~15 признаков)
Для каждого временного окна (1h, 12h, 24h):
- `tx_count_{window}` - количество транзакций в окне
- `tx_mean_amount_{window}` - средняя сумма в окне
- `tx_std_amount_{window}` - стандартное отклонение в окне
- `tx_sum_amount_{window}` - сумма всех транзакций в окне
- `tx_max_amount_{window}` - максимальная сумма в окне

**Назначение:** Анализ активности пользователя в последние N часов

### 4. Статистические признаки (~10+ признаков)
- `amount_zscore` - z-score относительно глобального распределения
- `amount_zscore_user` - z-score относительно пользователя
- `amount_percentile` - процентиль суммы
- `amount_ratio_to_max_{window}` - отношение к максимуму в окне
- `amount_ratio_to_mean_{window}` - отношение к среднему в окне
- `cv_amount_{window}` - коэффициент вариации (std/mean) для каждого окна

**Назначение:** Выявление статистических аномалий

### 5. Device/OS/IP/Geo признаки (зависит от данных)
- `device_freq` - частота использования устройства
- `user_unique_devices` - количество уникальных устройств пользователя
- `device_changed` - флаг изменения устройства (0/1)
- `os_freq` - частота использования OS версии
- `os_changed` - флаг изменения OS (0/1)
- `ip_freq` - частота использования IP (если есть)
- `user_unique_ips` - количество уникальных IP (если есть)
- `ip_changed` - флаг изменения IP (если есть)
- `geo_freq`, `geo_changed` - аналогично для геолокации (если есть)

**Назначение:** Выявление подозрительных изменений устройств/IP/локаций

### 6. Признаки поведения (~10+ признаков)
- `time_since_last_tx` - время с последней транзакции (секунды)
- `time_until_next_tx` - время до следующей транзакции (секунды)
- `tx_rate_per_hour_{window}` - скорость транзакций (транзакций/час)
- `avg_interval_{window}` - средний интервал между транзакциями
- `is_rapid_tx` - флаг быстрых транзакций (< 60 сек) (0/1)
- `interval_anomaly` - флаг аномального интервала (0/1)
- `tx_in_morning` - транзакция утром 6-12 (0/1)
- `tx_in_afternoon` - транзакция днем 12-18 (0/1)
- `tx_in_evening` - транзакция вечером 18-22 (0/1)

**Назначение:** Выявление аномалий в скорости и паттернах транзакций

### 7. Категориальные признаки
- `recipient_freq_encoding` - frequency encoding для получателя (если есть)

### 8. Поведенческие признаки из исходных данных
Все признаки из таблицы "поведенческие паттерны клиентов" также включены в финальный датасет.

## ⚙️ Предобработка

- **Масштабирование:** Все числовые признаки (кроме циклических sin/cos и бинарных) масштабированы с помощью MinMaxScaler
- **Пропуски:** Заполнены нулями
- **Временные колонки:** `timestamp` удален, все временные признаки извлечены

## 📝 Рекомендации для ML инжинера

1. **Дисбаланс классов:** Используйте метрики Precision, Recall, F1-score, ROC-AUC
2. **Техники балансировки:** SMOTE, undersampling, или class_weight в моделях
3. **Feature selection:** Рассмотрите удаление признаков с высокой корреляцией (см. EDA ноутбук)
4. **Валидация:** Используйте временную валидацию (train на старых данных, test на новых)
5. **Модели:** Рекомендуется попробовать:
   - LightGBM / XGBoost (хорошо работают с табличными данными)
   - Random Forest
   - Logistic Regression (как baseline)

## 📁 Файлы проекта

- `notebooks/01_data_overview.ipynb` - EDA анализ (задачи 11-22)
- `notebooks/01_full_data_pipeline.ipynb` - Feature Engineering пайплайн (задачи 23-28)
- `data/processed/transactions_with_features.csv` - **ФИНАЛЬНЫЙ ДАТАСЕТ ДЛЯ ОБУЧЕНИЯ**

## ✅ Готово к использованию

Все признаки созданы, масштабированы и готовы для обучения модели. Датасет сохранен в `data/processed/transactions_with_features.csv`.


