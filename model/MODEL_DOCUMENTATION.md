# 📋 Документация модели Fraud Detection

## Общая информация

- **Модель:** LightGBM (Gradient Boosting)
- **Версия:** 1.0.0
- **Дата сборки:** 2025-11-24
- **Файлы:** `model/final_model.pkl`, `model/preprocessing_pipeline.pkl`, `model/selected_features.pkl`, `model/threshold.pkl`

Модель обучена на транзакциях мобильного интернет-банкинга с учётом пользовательских и поведенческих признаков. Используется в API (`src/api/predict.py`) и вызывается через эндпоинты `/predict` и `/explain`.

## Метрики производительности

| Метрика    | Значение |
|-----------|----------|
| ROC-AUC   | 0.9765   |
| F2-score  | 0.5917   |
| Recall    | 0.6061   |
| Precision | 0.5405   |
| F1-score  | 0.5714   |

> **Главная метрика:** F2-score (фокус на Recall, чтобы не пропустить мошеннические транзакции).

## Оптимальный порог

- **Threshold:** `0.550`
- Класс определяется как мошеннический, если `fraud_probability >= threshold`.

## Входные данные

Модель ожидает 83 числовых признака (список хранится в `selected_features.pkl`). Основные группы:

- **Временные признаки:** `hour`, `day_of_week`, `month`, синусы/косинусы.
- **По пользователю:** статистика сумм и количества транзакций.
- **Rolling-окна:** активность пользователя за 1h/12h/24h.
- **Device/OS:** частота смен устройств, OS и др.
- **Поведенческие признаки:** время с последней транзакции, скорость операций и т.д.

> Требования: порядок признаков должен совпадать с `selected_features`. Пропуски следует заменять нулями или медианными значениями. Для удобства используйте `src/ml/preprocess.py`.

## Выходные данные

1. **Вероятность мошенничества** (float от 0 до 1).
2. **Класс** (bool): `True` — мошенничество, `False` — валидная транзакция.
3. **Статус** (`fraud` / `legitimate`) и **confidence** (`high`/`medium`/`low`) формируются API.

## Пример использования

```python
import joblib
import pandas as pd

model_pkg = joblib.load("model/final_model.pkl")
model = model_pkg["model"]
selected_features = model_pkg["selected_features"]
threshold = model_pkg["threshold"]

X = pd.DataFrame([...], columns=selected_features)
proba = model.predict_proba(X)[:, 1]
preds = proba >= threshold
```

## SHAP и интерпретация

- В API подключена библиотека SHAP (`/explain`), используется `shap.TreeExplainer`.
- Для каждой транзакции возвращается список факторов (`top_factors`) в порядке влияния.

## Обновление модели

1. Выполнить feature engineering (`run_feature_engineering.py` или ноутбуки).
2. Запустить `python train_model.py`.
3. Проверить метрики и визуализации в `model/*.png`.
4. Обновить эти файлы и README при изменении метрик или структуры признаков.


